name: Generate Daily Int Brief - Paused

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  daily-int-brief-paused:
    runs-on: ubuntu-latest

    steps:
      - name: Daily Int Brief paused
        run: |
          echo "The old Daily Int Brief workflow is paused."
          echo "Use Generate Daily Vulnerability Brief for vulnerability updates."
          echo "This workflow no longer runs automatically."
